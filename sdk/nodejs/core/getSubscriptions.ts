// *** WARNING: this file was generated by the Kulado Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as kulado from "@kulado/kulado";
import * as utilities from "../utilities";

/**
 * Use this data source to access information about all the Subscriptions currently available.
 * 
 * ## Example Usage
 * 
 * ```typescript
 * import * as kulado from "@kulado/kulado";
 * import * as azure from "@kulado/azure";
 * 
 * const available = kulado.output(azure.core.getSubscriptions({}));
 * 
 * export const availableSubscriptions = available.subscriptions;
 * export const firstAvailableSubscriptionDisplayName = available.subscriptions[0].displayName;
 * ```
 *
 * > This content is derived from https://github.com/terraform-providers/terraform-provider-azurerm/blob/master/website/docs/d/subscriptions.html.markdown.
 */
export function getSubscriptions(args?: GetSubscriptionsArgs, opts?: kulado.InvokeOptions): Promise<GetSubscriptionsResult> & GetSubscriptionsResult {
    args = args || {};
    const promise: Promise<GetSubscriptionsResult> = kulado.runtime.invoke("azure:core/getSubscriptions:getSubscriptions", {
        "displayNameContains": args.displayNameContains,
        "displayNamePrefix": args.displayNamePrefix,
    }, opts);

    return kulado.utils.liftProperties(promise, opts);
}

/**
 * A collection of arguments for invoking getSubscriptions.
 */
export interface GetSubscriptionsArgs {
    /**
     * A case-insensitive value which must be contained within the `display_name` field, used to filter the results
     */
    readonly displayNameContains?: string;
    /**
     * A case-insensitive prefix which can be used to filter on the `display_name` field
     */
    readonly displayNamePrefix?: string;
}

/**
 * A collection of values returned by getSubscriptions.
 */
export interface GetSubscriptionsResult {
    readonly displayNameContains?: string;
    readonly displayNamePrefix?: string;
    /**
     * One or more `subscription` blocks as defined below.
     */
    readonly subscriptions: { displayName: string, locationPlacementId: string, quotaId: string, spendingLimit: string, state: string, subscriptionId: string }[];
    /**
     * id is the provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
}
