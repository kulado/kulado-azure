// *** WARNING: this file was generated by the Kulado Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as kulado from "@kulado/kulado";
import * as utilities from "../utilities";

/**
 * Use this data source to access information about an existing Resource Group.
 * 
 * ## Example Usage
 * 
 * ```typescript
 * import * as kulado from "@kulado/kulado";
 * import * as azure from "@kulado/azure";
 * 
 * const testResourceGroup = kulado.output(azure.core.getResourceGroup({
 *     name: "dsrg_test",
 * }));
 * const testManagedDisk = new azure.compute.ManagedDisk("test", {
 *     createOption: "Empty",
 *     diskSizeGb: 1,
 *     location: testResourceGroup.location,
 *     name: "managed_disk_name",
 *     resourceGroupName: testResourceGroup.name,
 *     storageAccountType: "Standard_LRS",
 * });
 * ```
 *
 * > This content is derived from https://github.com/terraform-providers/terraform-provider-azurerm/blob/master/website/docs/d/resource_group.html.markdown.
 */
export function getResourceGroup(args: GetResourceGroupArgs, opts?: kulado.InvokeOptions): Promise<GetResourceGroupResult> & GetResourceGroupResult {
    const promise: Promise<GetResourceGroupResult> = kulado.runtime.invoke("azure:core/getResourceGroup:getResourceGroup", {
        "name": args.name,
    }, opts);

    return kulado.utils.liftProperties(promise, opts);
}

/**
 * A collection of arguments for invoking getResourceGroup.
 */
export interface GetResourceGroupArgs {
    /**
     * Specifies the name of the resource group.
     */
    readonly name: string;
}

/**
 * A collection of values returned by getResourceGroup.
 */
export interface GetResourceGroupResult {
    /**
     * The location of the resource group.
     */
    readonly location: string;
    readonly name: string;
    /**
     * A mapping of tags assigned to the resource group.
     */
    readonly tags: {[key: string]: any};
    /**
     * id is the provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
}
