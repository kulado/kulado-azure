// *** WARNING: this file was generated by the Kulado Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as kulado from "@kulado/kulado";
import * as utilities from "../utilities";

/**
 * Use this data source to access the configuration of the AzureRM provider.
 * 
 * ## Example Usage
 * 
 * ```typescript
 * import * as kulado from "@kulado/kulado";
 * import * as azure from "@kulado/azure";
 * 
 * const current = kulado.output(azure.core.getClientConfig({}));
 * 
 * export const accountId = current.servicePrincipalApplicationId;
 * ```
 *
 * > This content is derived from https://github.com/terraform-providers/terraform-provider-azurerm/blob/master/website/docs/d/client_config.html.markdown.
 */
export function getClientConfig(opts?: kulado.InvokeOptions): Promise<GetClientConfigResult> & GetClientConfigResult {
    const promise: Promise<GetClientConfigResult> = kulado.runtime.invoke("azure:core/getClientConfig:getClientConfig", {
    }, opts);

    return kulado.utils.liftProperties(promise, opts);
}

/**
 * A collection of values returned by getClientConfig.
 */
export interface GetClientConfigResult {
    readonly clientId: string;
    readonly servicePrincipalApplicationId: string;
    readonly servicePrincipalObjectId: string;
    readonly subscriptionId: string;
    readonly tenantId: string;
    /**
     * id is the provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
}
