// *** WARNING: this file was generated by the Kulado Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as kulado from "@kulado/kulado";
import * as utilities from "../utilities";

/**
 * Manages an Action Group within Azure Monitor.
 * 
 * ## Example Usage
 * 
 * ```typescript
 * import * as kulado from "@kulado/kulado";
 * import * as azure from "@kulado/azure";
 * 
 * const testResourceGroup = new azure.core.ResourceGroup("test", {
 *     location: "West US",
 *     name: "monitoring-resources",
 * });
 * const testActionGroup = new azure.monitoring.ActionGroup("test", {
 *     emailReceivers: [
 *         {
 *             emailAddress: "admin@contoso.com",
 *             name: "sendtoadmin",
 *         },
 *         {
 *             emailAddress: "devops@contoso.com",
 *             name: "sendtodevops",
 *         },
 *     ],
 *     name: "CriticalAlertsAction",
 *     resourceGroupName: testResourceGroup.name,
 *     shortName: "p0action",
 *     smsReceivers: [{
 *         countryCode: "1",
 *         name: "oncallmsg",
 *         phoneNumber: "1231231234",
 *     }],
 *     webhookReceivers: [{
 *         name: "callmyapiaswell",
 *         serviceUri: "http://example.com/alert",
 *     }],
 * });
 * ```
 *
 * > This content is derived from https://github.com/terraform-providers/terraform-provider-azurerm/blob/master/website/docs/r/monitor_action_group.html.markdown.
 */
export class ActionGroup extends kulado.CustomResource {
    /**
     * Get an existing ActionGroup resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     */
    public static get(name: string, id: kulado.Input<kulado.ID>, state?: ActionGroupState, opts?: kulado.CustomResourceOptions): ActionGroup {
        return new ActionGroup(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __kuladoType = 'azure:monitoring/actionGroup:ActionGroup';

    /**
     * Returns true if the given object is an instance of ActionGroup.  This is designed to work even
     * when multiple copies of the Kulado SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ActionGroup {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__kuladoType'] === ActionGroup.__kuladoType;
    }

    /**
     * One or more `email_receiver` blocks as defined below.
     */
    public readonly emailReceivers!: kulado.Output<{ emailAddress: string, name: string }[] | undefined>;
    /**
     * Whether this action group is enabled. If an action group is not enabled, then none of its receivers will receive communications. Defaults to `true`.
     */
    public readonly enabled!: kulado.Output<boolean | undefined>;
    /**
     * The name of the webhook receiver. Names must be unique (case-insensitive) across all receivers within an action group.
     */
    public readonly name!: kulado.Output<string>;
    /**
     * The name of the resource group in which to create the Action Group instance.
     */
    public readonly resourceGroupName!: kulado.Output<string>;
    /**
     * The short name of the action group. This will be used in SMS messages.
     */
    public readonly shortName!: kulado.Output<string>;
    /**
     * One or more `sms_receiver ` blocks as defined below.
     */
    public readonly smsReceivers!: kulado.Output<{ countryCode: string, name: string, phoneNumber: string }[] | undefined>;
    /**
     * A mapping of tags to assign to the resource.
     */
    public readonly tags!: kulado.Output<{[key: string]: any}>;
    /**
     * One or more `webhook_receiver ` blocks as defined below.
     */
    public readonly webhookReceivers!: kulado.Output<{ name: string, serviceUri: string }[] | undefined>;

    /**
     * Create a ActionGroup resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ActionGroupArgs, opts?: kulado.CustomResourceOptions)
    constructor(name: string, argsOrState?: ActionGroupArgs | ActionGroupState, opts?: kulado.CustomResourceOptions) {
        let inputs: kulado.Inputs = {};
        if (opts && opts.id) {
            const state = argsOrState as ActionGroupState | undefined;
            inputs["emailReceivers"] = state ? state.emailReceivers : undefined;
            inputs["enabled"] = state ? state.enabled : undefined;
            inputs["name"] = state ? state.name : undefined;
            inputs["resourceGroupName"] = state ? state.resourceGroupName : undefined;
            inputs["shortName"] = state ? state.shortName : undefined;
            inputs["smsReceivers"] = state ? state.smsReceivers : undefined;
            inputs["tags"] = state ? state.tags : undefined;
            inputs["webhookReceivers"] = state ? state.webhookReceivers : undefined;
        } else {
            const args = argsOrState as ActionGroupArgs | undefined;
            if (!args || args.resourceGroupName === undefined) {
                throw new Error("Missing required property 'resourceGroupName'");
            }
            if (!args || args.shortName === undefined) {
                throw new Error("Missing required property 'shortName'");
            }
            inputs["emailReceivers"] = args ? args.emailReceivers : undefined;
            inputs["enabled"] = args ? args.enabled : undefined;
            inputs["name"] = args ? args.name : undefined;
            inputs["resourceGroupName"] = args ? args.resourceGroupName : undefined;
            inputs["shortName"] = args ? args.shortName : undefined;
            inputs["smsReceivers"] = args ? args.smsReceivers : undefined;
            inputs["tags"] = args ? args.tags : undefined;
            inputs["webhookReceivers"] = args ? args.webhookReceivers : undefined;
        }
        super(ActionGroup.__kuladoType, name, inputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ActionGroup resources.
 */
export interface ActionGroupState {
    /**
     * One or more `email_receiver` blocks as defined below.
     */
    readonly emailReceivers?: kulado.Input<kulado.Input<{ emailAddress: kulado.Input<string>, name: kulado.Input<string> }>[]>;
    /**
     * Whether this action group is enabled. If an action group is not enabled, then none of its receivers will receive communications. Defaults to `true`.
     */
    readonly enabled?: kulado.Input<boolean>;
    /**
     * The name of the webhook receiver. Names must be unique (case-insensitive) across all receivers within an action group.
     */
    readonly name?: kulado.Input<string>;
    /**
     * The name of the resource group in which to create the Action Group instance.
     */
    readonly resourceGroupName?: kulado.Input<string>;
    /**
     * The short name of the action group. This will be used in SMS messages.
     */
    readonly shortName?: kulado.Input<string>;
    /**
     * One or more `sms_receiver ` blocks as defined below.
     */
    readonly smsReceivers?: kulado.Input<kulado.Input<{ countryCode: kulado.Input<string>, name: kulado.Input<string>, phoneNumber: kulado.Input<string> }>[]>;
    /**
     * A mapping of tags to assign to the resource.
     */
    readonly tags?: kulado.Input<{[key: string]: any}>;
    /**
     * One or more `webhook_receiver ` blocks as defined below.
     */
    readonly webhookReceivers?: kulado.Input<kulado.Input<{ name: kulado.Input<string>, serviceUri: kulado.Input<string> }>[]>;
}

/**
 * The set of arguments for constructing a ActionGroup resource.
 */
export interface ActionGroupArgs {
    /**
     * One or more `email_receiver` blocks as defined below.
     */
    readonly emailReceivers?: kulado.Input<kulado.Input<{ emailAddress: kulado.Input<string>, name: kulado.Input<string> }>[]>;
    /**
     * Whether this action group is enabled. If an action group is not enabled, then none of its receivers will receive communications. Defaults to `true`.
     */
    readonly enabled?: kulado.Input<boolean>;
    /**
     * The name of the webhook receiver. Names must be unique (case-insensitive) across all receivers within an action group.
     */
    readonly name?: kulado.Input<string>;
    /**
     * The name of the resource group in which to create the Action Group instance.
     */
    readonly resourceGroupName: kulado.Input<string>;
    /**
     * The short name of the action group. This will be used in SMS messages.
     */
    readonly shortName: kulado.Input<string>;
    /**
     * One or more `sms_receiver ` blocks as defined below.
     */
    readonly smsReceivers?: kulado.Input<kulado.Input<{ countryCode: kulado.Input<string>, name: kulado.Input<string>, phoneNumber: kulado.Input<string> }>[]>;
    /**
     * A mapping of tags to assign to the resource.
     */
    readonly tags?: kulado.Input<{[key: string]: any}>;
    /**
     * One or more `webhook_receiver ` blocks as defined below.
     */
    readonly webhookReceivers?: kulado.Input<kulado.Input<{ name: kulado.Input<string>, serviceUri: kulado.Input<string> }>[]>;
}
