// *** WARNING: this file was generated by the Kulado Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as kulado from "@kulado/kulado";
import * as utilities from "../utilities";

/**
 * Manages a NAT Rule Collection within an Azure Firewall.
 * 
 * ## Example Usage
 * 
 * ```typescript
 * import * as kulado from "@kulado/kulado";
 * import * as azure from "@kulado/azure";
 * 
 * const testResourceGroup = new azure.core.ResourceGroup("test", {
 *     location: "North Europe",
 *     name: "example-resources",
 * });
 * const testPublicIp = new azure.network.PublicIp("test", {
 *     allocationMethod: "Static",
 *     location: testResourceGroup.location,
 *     name: "testpip",
 *     resourceGroupName: testResourceGroup.name,
 *     sku: "Standard",
 * });
 * const testVirtualNetwork = new azure.network.VirtualNetwork("test", {
 *     addressSpaces: ["10.0.0.0/16"],
 *     location: testResourceGroup.location,
 *     name: "testvnet",
 *     resourceGroupName: testResourceGroup.name,
 * });
 * const testSubnet = new azure.network.Subnet("test", {
 *     addressPrefix: "10.0.1.0/24",
 *     name: "AzureFirewallSubnet",
 *     resourceGroupName: testResourceGroup.name,
 *     virtualNetworkName: testVirtualNetwork.name,
 * });
 * const testFirewall = new azure.network.Firewall("test", {
 *     ipConfiguration: {
 *         name: "configuration",
 *         publicIpAddressId: testPublicIp.id,
 *         subnetId: testSubnet.id,
 *     },
 *     location: testResourceGroup.location,
 *     name: "testfirewall",
 *     resourceGroupName: testResourceGroup.name,
 * });
 * const testFirewallNatRuleCollection = new azure.network.FirewallNatRuleCollection("test", {
 *     action: "Dnat",
 *     azureFirewallName: testFirewall.name,
 *     name: "testcollection",
 *     priority: 100,
 *     resourceGroupName: testResourceGroup.name,
 *     rules: [{
 *         destinationAddresses: [
 *             "8.8.8.8",
 *             "8.8.4.4",
 *         ],
 *         destinationPorts: ["53"],
 *         name: "testrule",
 *         protocols: [
 *             "TCP",
 *             "UDP",
 *         ],
 *         sourceAddresses: ["10.0.0.0/16"],
 *     }],
 * });
 * ```
 *
 * > This content is derived from https://github.com/terraform-providers/terraform-provider-azurerm/blob/master/website/docs/r/firewall_nat_rule_collection.html.markdown.
 */
export class FirewallNatRuleCollection extends kulado.CustomResource {
    /**
     * Get an existing FirewallNatRuleCollection resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     */
    public static get(name: string, id: kulado.Input<kulado.ID>, state?: FirewallNatRuleCollectionState, opts?: kulado.CustomResourceOptions): FirewallNatRuleCollection {
        return new FirewallNatRuleCollection(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __kuladoType = 'azure:network/firewallNatRuleCollection:FirewallNatRuleCollection';

    /**
     * Returns true if the given object is an instance of FirewallNatRuleCollection.  This is designed to work even
     * when multiple copies of the Kulado SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is FirewallNatRuleCollection {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__kuladoType'] === FirewallNatRuleCollection.__kuladoType;
    }

    /**
     * Specifies the action the rule will apply to matching traffic. Possible values are `Dnat` and `Snat`.
     */
    public readonly action!: kulado.Output<string>;
    /**
     * Specifies the name of the Firewall in which the NAT Rule Collection should be created. Changing this forces a new resource to be created.
     */
    public readonly azureFirewallName!: kulado.Output<string>;
    /**
     * Specifies the name of the NAT Rule Collection which must be unique within the Firewall. Changing this forces a new resource to be created.
     */
    public readonly name!: kulado.Output<string>;
    /**
     * Specifies the priority of the rule collection. Possible values are between `100` - `65000`.
     */
    public readonly priority!: kulado.Output<number>;
    /**
     * Specifies the name of the Resource Group in which the Firewall exists. Changing this forces a new resource to be created.
     */
    public readonly resourceGroupName!: kulado.Output<string>;
    /**
     * One or more `rule` blocks as defined below.
     */
    public readonly rules!: kulado.Output<{ description?: string, destinationAddresses: string[], destinationPorts: string[], name: string, protocols: string[], sourceAddresses: string[], translatedAddress: string, translatedPort: string }[]>;

    /**
     * Create a FirewallNatRuleCollection resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: FirewallNatRuleCollectionArgs, opts?: kulado.CustomResourceOptions)
    constructor(name: string, argsOrState?: FirewallNatRuleCollectionArgs | FirewallNatRuleCollectionState, opts?: kulado.CustomResourceOptions) {
        let inputs: kulado.Inputs = {};
        if (opts && opts.id) {
            const state = argsOrState as FirewallNatRuleCollectionState | undefined;
            inputs["action"] = state ? state.action : undefined;
            inputs["azureFirewallName"] = state ? state.azureFirewallName : undefined;
            inputs["name"] = state ? state.name : undefined;
            inputs["priority"] = state ? state.priority : undefined;
            inputs["resourceGroupName"] = state ? state.resourceGroupName : undefined;
            inputs["rules"] = state ? state.rules : undefined;
        } else {
            const args = argsOrState as FirewallNatRuleCollectionArgs | undefined;
            if (!args || args.action === undefined) {
                throw new Error("Missing required property 'action'");
            }
            if (!args || args.azureFirewallName === undefined) {
                throw new Error("Missing required property 'azureFirewallName'");
            }
            if (!args || args.priority === undefined) {
                throw new Error("Missing required property 'priority'");
            }
            if (!args || args.resourceGroupName === undefined) {
                throw new Error("Missing required property 'resourceGroupName'");
            }
            if (!args || args.rules === undefined) {
                throw new Error("Missing required property 'rules'");
            }
            inputs["action"] = args ? args.action : undefined;
            inputs["azureFirewallName"] = args ? args.azureFirewallName : undefined;
            inputs["name"] = args ? args.name : undefined;
            inputs["priority"] = args ? args.priority : undefined;
            inputs["resourceGroupName"] = args ? args.resourceGroupName : undefined;
            inputs["rules"] = args ? args.rules : undefined;
        }
        super(FirewallNatRuleCollection.__kuladoType, name, inputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering FirewallNatRuleCollection resources.
 */
export interface FirewallNatRuleCollectionState {
    /**
     * Specifies the action the rule will apply to matching traffic. Possible values are `Dnat` and `Snat`.
     */
    readonly action?: kulado.Input<string>;
    /**
     * Specifies the name of the Firewall in which the NAT Rule Collection should be created. Changing this forces a new resource to be created.
     */
    readonly azureFirewallName?: kulado.Input<string>;
    /**
     * Specifies the name of the NAT Rule Collection which must be unique within the Firewall. Changing this forces a new resource to be created.
     */
    readonly name?: kulado.Input<string>;
    /**
     * Specifies the priority of the rule collection. Possible values are between `100` - `65000`.
     */
    readonly priority?: kulado.Input<number>;
    /**
     * Specifies the name of the Resource Group in which the Firewall exists. Changing this forces a new resource to be created.
     */
    readonly resourceGroupName?: kulado.Input<string>;
    /**
     * One or more `rule` blocks as defined below.
     */
    readonly rules?: kulado.Input<kulado.Input<{ description?: kulado.Input<string>, destinationAddresses: kulado.Input<kulado.Input<string>[]>, destinationPorts: kulado.Input<kulado.Input<string>[]>, name: kulado.Input<string>, protocols: kulado.Input<kulado.Input<string>[]>, sourceAddresses: kulado.Input<kulado.Input<string>[]>, translatedAddress: kulado.Input<string>, translatedPort: kulado.Input<string> }>[]>;
}

/**
 * The set of arguments for constructing a FirewallNatRuleCollection resource.
 */
export interface FirewallNatRuleCollectionArgs {
    /**
     * Specifies the action the rule will apply to matching traffic. Possible values are `Dnat` and `Snat`.
     */
    readonly action: kulado.Input<string>;
    /**
     * Specifies the name of the Firewall in which the NAT Rule Collection should be created. Changing this forces a new resource to be created.
     */
    readonly azureFirewallName: kulado.Input<string>;
    /**
     * Specifies the name of the NAT Rule Collection which must be unique within the Firewall. Changing this forces a new resource to be created.
     */
    readonly name?: kulado.Input<string>;
    /**
     * Specifies the priority of the rule collection. Possible values are between `100` - `65000`.
     */
    readonly priority: kulado.Input<number>;
    /**
     * Specifies the name of the Resource Group in which the Firewall exists. Changing this forces a new resource to be created.
     */
    readonly resourceGroupName: kulado.Input<string>;
    /**
     * One or more `rule` blocks as defined below.
     */
    readonly rules: kulado.Input<kulado.Input<{ description?: kulado.Input<string>, destinationAddresses: kulado.Input<kulado.Input<string>[]>, destinationPorts: kulado.Input<kulado.Input<string>[]>, name: kulado.Input<string>, protocols: kulado.Input<kulado.Input<string>[]>, sourceAddresses: kulado.Input<kulado.Input<string>[]>, translatedAddress: kulado.Input<string>, translatedPort: kulado.Input<string> }>[]>;
}
