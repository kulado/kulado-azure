// *** WARNING: this file was generated by the Kulado Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as kulado from "@kulado/kulado";
import * as utilities from "../utilities";

/**
 * Use this data source to access information about an existing Load Balancer
 * 
 * ## Example Usage
 * 
 * ```typescript
 * import * as kulado from "@kulado/kulado";
 * import * as azure from "@kulado/azure";
 * 
 * const test = kulado.output(azure.lb.getLB({
 *     name: "example-lb",
 *     resourceGroupName: "example-resources",
 * }));
 * 
 * export const loadbalancerId = test.id;
 * ```
 *
 * > This content is derived from https://github.com/terraform-providers/terraform-provider-azurerm/blob/master/website/docs/d/lb.html.markdown.
 */
export function getLB(args: GetLBArgs, opts?: kulado.InvokeOptions): Promise<GetLBResult> & GetLBResult {
    const promise: Promise<GetLBResult> = kulado.runtime.invoke("azure:lb/getLB:getLB", {
        "name": args.name,
        "resourceGroupName": args.resourceGroupName,
    }, opts);

    return kulado.utils.liftProperties(promise, opts);
}

/**
 * A collection of arguments for invoking getLB.
 */
export interface GetLBArgs {
    /**
     * Specifies the name of the Load Balancer.
     */
    readonly name: string;
    /**
     * The name of the Resource Group in which the Load Balancer exists.
     */
    readonly resourceGroupName: string;
}

/**
 * A collection of values returned by getLB.
 */
export interface GetLBResult {
    /**
     * (Optional) A `frontend_ip_configuration` block as documented below.
     */
    readonly frontendIpConfigurations: { name: string, privateIpAddress: string, privateIpAddressAllocation: string, publicIpAddressId: string, subnetId: string, zones: string[] }[];
    /**
     * The Azure location where the Load Balancer exists.
     */
    readonly location: string;
    /**
     * The name of the Frontend IP Configuration.
     */
    readonly name: string;
    /**
     * Private IP Address to assign to the Load Balancer.
     */
    readonly privateIpAddress: string;
    /**
     * The list of private IP address assigned to the load balancer in `frontend_ip_configuration` blocks, if any.
     */
    readonly privateIpAddresses: string[];
    readonly resourceGroupName: string;
    /**
     * The SKU of the Load Balancer.
     */
    readonly sku: string;
    /**
     * A mapping of tags assigned to the resource.
     */
    readonly tags: {[key: string]: any};
    /**
     * id is the provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
}
