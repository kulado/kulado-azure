// *** WARNING: this file was generated by the Kulado Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as kulado from "@kulado/kulado";
import * as utilities from "../utilities";

/**
 * Manages a Load Balancer Rule.
 * 
 * > **NOTE** When using this resource, the Load Balancer needs to have a FrontEnd IP Configuration Attached
 * 
 * ## Example Usage
 * 
 * ```typescript
 * import * as kulado from "@kulado/kulado";
 * import * as azure from "@kulado/azure";
 * 
 * const testResourceGroup = new azure.core.ResourceGroup("test", {
 *     location: "West US",
 *     name: "LoadBalancerRG",
 * });
 * const testPublicIp = new azure.network.PublicIp("test", {
 *     allocationMethod: "Static",
 *     location: "West US",
 *     name: "PublicIPForLB",
 *     resourceGroupName: testResourceGroup.name,
 * });
 * const testLoadBalancer = new azure.lb.LoadBalancer("test", {
 *     frontendIpConfigurations: [{
 *         name: "PublicIPAddress",
 *         publicIpAddressId: testPublicIp.id,
 *     }],
 *     location: "West US",
 *     name: "TestLoadBalancer",
 *     resourceGroupName: testResourceGroup.name,
 * });
 * const testRule = new azure.lb.Rule("test", {
 *     backendPort: 3389,
 *     frontendIpConfigurationName: "PublicIPAddress",
 *     frontendPort: 3389,
 *     loadbalancerId: testLoadBalancer.id,
 *     name: "LBRule",
 *     protocol: "Tcp",
 *     resourceGroupName: testResourceGroup.name,
 * });
 * ```
 *
 * > This content is derived from https://github.com/terraform-providers/terraform-provider-azurerm/blob/master/website/docs/r/lb_rule.html.markdown.
 */
export class Rule extends kulado.CustomResource {
    /**
     * Get an existing Rule resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     */
    public static get(name: string, id: kulado.Input<kulado.ID>, state?: RuleState, opts?: kulado.CustomResourceOptions): Rule {
        return new Rule(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __kuladoType = 'azure:lb/rule:Rule';

    /**
     * Returns true if the given object is an instance of Rule.  This is designed to work even
     * when multiple copies of the Kulado SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Rule {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__kuladoType'] === Rule.__kuladoType;
    }

    /**
     * A reference to a Backend Address Pool over which this Load Balancing Rule operates.
     */
    public readonly backendAddressPoolId!: kulado.Output<string>;
    /**
     * The port used for internal connections on the endpoint. Possible values range between 0 and 65535, inclusive.
     */
    public readonly backendPort!: kulado.Output<number>;
    /**
     * Indicates whether outbound snat is disabled or enabled. Default false.
     */
    public readonly disableOutboundSnat!: kulado.Output<boolean | undefined>;
    /**
     * Floating IP is pertinent to failover scenarios: a "floating” IP is reassigned to a secondary server in case the primary server fails. Floating IP is required for SQL AlwaysOn.
     */
    public readonly enableFloatingIp!: kulado.Output<boolean | undefined>;
    public /*out*/ readonly frontendIpConfigurationId!: kulado.Output<string>;
    /**
     * The name of the frontend IP configuration to which the rule is associated.
     */
    public readonly frontendIpConfigurationName!: kulado.Output<string>;
    /**
     * The port for the external endpoint. Port numbers for each Rule must be unique within the Load Balancer. Possible values range between 0 and 65534, inclusive.
     */
    public readonly frontendPort!: kulado.Output<number>;
    /**
     * Specifies the timeout for the Tcp idle connection. The value can be set between 4 and 30 minutes. The default value is 4 minutes. This element is only used when the protocol is set to Tcp.
     */
    public readonly idleTimeoutInMinutes!: kulado.Output<number>;
    /**
     * Specifies the load balancing distribution type to be used by the Load Balancer. Possible values are: `Default` – The load balancer is configured to use a 5 tuple hash to map traffic to available servers. `SourceIP` – The load balancer is configured to use a 2 tuple hash to map traffic to available servers. `SourceIPProtocol` – The load balancer is configured to use a 3 tuple hash to map traffic to available servers. Also known as Session Persistence, where  the options are called `None`, `Client IP` and `Client IP and Protocol` respectively.
     */
    public readonly loadDistribution!: kulado.Output<string>;
    /**
     * The ID of the Load Balancer in which to create the Rule.
     */
    public readonly loadbalancerId!: kulado.Output<string>;
    public readonly location!: kulado.Output<string>;
    /**
     * Specifies the name of the LB Rule.
     */
    public readonly name!: kulado.Output<string>;
    /**
     * A reference to a Probe used by this Load Balancing Rule.
     */
    public readonly probeId!: kulado.Output<string>;
    /**
     * The transport protocol for the external endpoint. Possible values are `Tcp`, `Udp` or `All`.
     */
    public readonly protocol!: kulado.Output<string>;
    /**
     * The name of the resource group in which to create the resource.
     */
    public readonly resourceGroupName!: kulado.Output<string>;

    /**
     * Create a Rule resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: RuleArgs, opts?: kulado.CustomResourceOptions)
    constructor(name: string, argsOrState?: RuleArgs | RuleState, opts?: kulado.CustomResourceOptions) {
        let inputs: kulado.Inputs = {};
        if (opts && opts.id) {
            const state = argsOrState as RuleState | undefined;
            inputs["backendAddressPoolId"] = state ? state.backendAddressPoolId : undefined;
            inputs["backendPort"] = state ? state.backendPort : undefined;
            inputs["disableOutboundSnat"] = state ? state.disableOutboundSnat : undefined;
            inputs["enableFloatingIp"] = state ? state.enableFloatingIp : undefined;
            inputs["frontendIpConfigurationId"] = state ? state.frontendIpConfigurationId : undefined;
            inputs["frontendIpConfigurationName"] = state ? state.frontendIpConfigurationName : undefined;
            inputs["frontendPort"] = state ? state.frontendPort : undefined;
            inputs["idleTimeoutInMinutes"] = state ? state.idleTimeoutInMinutes : undefined;
            inputs["loadDistribution"] = state ? state.loadDistribution : undefined;
            inputs["loadbalancerId"] = state ? state.loadbalancerId : undefined;
            inputs["location"] = state ? state.location : undefined;
            inputs["name"] = state ? state.name : undefined;
            inputs["probeId"] = state ? state.probeId : undefined;
            inputs["protocol"] = state ? state.protocol : undefined;
            inputs["resourceGroupName"] = state ? state.resourceGroupName : undefined;
        } else {
            const args = argsOrState as RuleArgs | undefined;
            if (!args || args.backendPort === undefined) {
                throw new Error("Missing required property 'backendPort'");
            }
            if (!args || args.frontendIpConfigurationName === undefined) {
                throw new Error("Missing required property 'frontendIpConfigurationName'");
            }
            if (!args || args.frontendPort === undefined) {
                throw new Error("Missing required property 'frontendPort'");
            }
            if (!args || args.loadbalancerId === undefined) {
                throw new Error("Missing required property 'loadbalancerId'");
            }
            if (!args || args.protocol === undefined) {
                throw new Error("Missing required property 'protocol'");
            }
            if (!args || args.resourceGroupName === undefined) {
                throw new Error("Missing required property 'resourceGroupName'");
            }
            inputs["backendAddressPoolId"] = args ? args.backendAddressPoolId : undefined;
            inputs["backendPort"] = args ? args.backendPort : undefined;
            inputs["disableOutboundSnat"] = args ? args.disableOutboundSnat : undefined;
            inputs["enableFloatingIp"] = args ? args.enableFloatingIp : undefined;
            inputs["frontendIpConfigurationName"] = args ? args.frontendIpConfigurationName : undefined;
            inputs["frontendPort"] = args ? args.frontendPort : undefined;
            inputs["idleTimeoutInMinutes"] = args ? args.idleTimeoutInMinutes : undefined;
            inputs["loadDistribution"] = args ? args.loadDistribution : undefined;
            inputs["loadbalancerId"] = args ? args.loadbalancerId : undefined;
            inputs["location"] = args ? args.location : undefined;
            inputs["name"] = args ? args.name : undefined;
            inputs["probeId"] = args ? args.probeId : undefined;
            inputs["protocol"] = args ? args.protocol : undefined;
            inputs["resourceGroupName"] = args ? args.resourceGroupName : undefined;
            inputs["frontendIpConfigurationId"] = undefined /*out*/;
        }
        super(Rule.__kuladoType, name, inputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Rule resources.
 */
export interface RuleState {
    /**
     * A reference to a Backend Address Pool over which this Load Balancing Rule operates.
     */
    readonly backendAddressPoolId?: kulado.Input<string>;
    /**
     * The port used for internal connections on the endpoint. Possible values range between 0 and 65535, inclusive.
     */
    readonly backendPort?: kulado.Input<number>;
    /**
     * Indicates whether outbound snat is disabled or enabled. Default false.
     */
    readonly disableOutboundSnat?: kulado.Input<boolean>;
    /**
     * Floating IP is pertinent to failover scenarios: a "floating” IP is reassigned to a secondary server in case the primary server fails. Floating IP is required for SQL AlwaysOn.
     */
    readonly enableFloatingIp?: kulado.Input<boolean>;
    readonly frontendIpConfigurationId?: kulado.Input<string>;
    /**
     * The name of the frontend IP configuration to which the rule is associated.
     */
    readonly frontendIpConfigurationName?: kulado.Input<string>;
    /**
     * The port for the external endpoint. Port numbers for each Rule must be unique within the Load Balancer. Possible values range between 0 and 65534, inclusive.
     */
    readonly frontendPort?: kulado.Input<number>;
    /**
     * Specifies the timeout for the Tcp idle connection. The value can be set between 4 and 30 minutes. The default value is 4 minutes. This element is only used when the protocol is set to Tcp.
     */
    readonly idleTimeoutInMinutes?: kulado.Input<number>;
    /**
     * Specifies the load balancing distribution type to be used by the Load Balancer. Possible values are: `Default` – The load balancer is configured to use a 5 tuple hash to map traffic to available servers. `SourceIP` – The load balancer is configured to use a 2 tuple hash to map traffic to available servers. `SourceIPProtocol` – The load balancer is configured to use a 3 tuple hash to map traffic to available servers. Also known as Session Persistence, where  the options are called `None`, `Client IP` and `Client IP and Protocol` respectively.
     */
    readonly loadDistribution?: kulado.Input<string>;
    /**
     * The ID of the Load Balancer in which to create the Rule.
     */
    readonly loadbalancerId?: kulado.Input<string>;
    readonly location?: kulado.Input<string>;
    /**
     * Specifies the name of the LB Rule.
     */
    readonly name?: kulado.Input<string>;
    /**
     * A reference to a Probe used by this Load Balancing Rule.
     */
    readonly probeId?: kulado.Input<string>;
    /**
     * The transport protocol for the external endpoint. Possible values are `Tcp`, `Udp` or `All`.
     */
    readonly protocol?: kulado.Input<string>;
    /**
     * The name of the resource group in which to create the resource.
     */
    readonly resourceGroupName?: kulado.Input<string>;
}

/**
 * The set of arguments for constructing a Rule resource.
 */
export interface RuleArgs {
    /**
     * A reference to a Backend Address Pool over which this Load Balancing Rule operates.
     */
    readonly backendAddressPoolId?: kulado.Input<string>;
    /**
     * The port used for internal connections on the endpoint. Possible values range between 0 and 65535, inclusive.
     */
    readonly backendPort: kulado.Input<number>;
    /**
     * Indicates whether outbound snat is disabled or enabled. Default false.
     */
    readonly disableOutboundSnat?: kulado.Input<boolean>;
    /**
     * Floating IP is pertinent to failover scenarios: a "floating” IP is reassigned to a secondary server in case the primary server fails. Floating IP is required for SQL AlwaysOn.
     */
    readonly enableFloatingIp?: kulado.Input<boolean>;
    /**
     * The name of the frontend IP configuration to which the rule is associated.
     */
    readonly frontendIpConfigurationName: kulado.Input<string>;
    /**
     * The port for the external endpoint. Port numbers for each Rule must be unique within the Load Balancer. Possible values range between 0 and 65534, inclusive.
     */
    readonly frontendPort: kulado.Input<number>;
    /**
     * Specifies the timeout for the Tcp idle connection. The value can be set between 4 and 30 minutes. The default value is 4 minutes. This element is only used when the protocol is set to Tcp.
     */
    readonly idleTimeoutInMinutes?: kulado.Input<number>;
    /**
     * Specifies the load balancing distribution type to be used by the Load Balancer. Possible values are: `Default` – The load balancer is configured to use a 5 tuple hash to map traffic to available servers. `SourceIP` – The load balancer is configured to use a 2 tuple hash to map traffic to available servers. `SourceIPProtocol` – The load balancer is configured to use a 3 tuple hash to map traffic to available servers. Also known as Session Persistence, where  the options are called `None`, `Client IP` and `Client IP and Protocol` respectively.
     */
    readonly loadDistribution?: kulado.Input<string>;
    /**
     * The ID of the Load Balancer in which to create the Rule.
     */
    readonly loadbalancerId: kulado.Input<string>;
    readonly location?: kulado.Input<string>;
    /**
     * Specifies the name of the LB Rule.
     */
    readonly name?: kulado.Input<string>;
    /**
     * A reference to a Probe used by this Load Balancing Rule.
     */
    readonly probeId?: kulado.Input<string>;
    /**
     * The transport protocol for the external endpoint. Possible values are `Tcp`, `Udp` or `All`.
     */
    readonly protocol: kulado.Input<string>;
    /**
     * The name of the resource group in which to create the resource.
     */
    readonly resourceGroupName: kulado.Input<string>;
}
