// *** WARNING: this file was generated by the Kulado Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as kulado from "@kulado/kulado";
import * as utilities from "../utilities";

/**
 * Manage a Load Balancer Resource.
 * 
 * ## Example Usage
 * 
 * ```typescript
 * import * as kulado from "@kulado/kulado";
 * import * as azure from "@kulado/azure";
 * 
 * const testResourceGroup = new azure.core.ResourceGroup("test", {
 *     location: "West US",
 *     name: "LoadBalancerRG",
 * });
 * const testPublicIp = new azure.network.PublicIp("test", {
 *     allocationMethod: "Static",
 *     location: "West US",
 *     name: "PublicIPForLB",
 *     resourceGroupName: testResourceGroup.name,
 * });
 * const testLoadBalancer = new azure.lb.LoadBalancer("test", {
 *     frontendIpConfigurations: [{
 *         name: "PublicIPAddress",
 *         publicIpAddressId: testPublicIp.id,
 *     }],
 *     location: "West US",
 *     name: "TestLoadBalancer",
 *     resourceGroupName: testResourceGroup.name,
 * });
 * ```
 *
 * > This content is derived from https://github.com/terraform-providers/terraform-provider-azurerm/blob/master/website/docs/r/lb.html.markdown.
 */
export class LoadBalancer extends kulado.CustomResource {
    /**
     * Get an existing LoadBalancer resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     */
    public static get(name: string, id: kulado.Input<kulado.ID>, state?: LoadBalancerState, opts?: kulado.CustomResourceOptions): LoadBalancer {
        return new LoadBalancer(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __kuladoType = 'azure:lb/loadBalancer:LoadBalancer';

    /**
     * Returns true if the given object is an instance of LoadBalancer.  This is designed to work even
     * when multiple copies of the Kulado SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is LoadBalancer {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__kuladoType'] === LoadBalancer.__kuladoType;
    }

    /**
     * One or multiple `frontend_ip_configuration` blocks as documented below.
     */
    public readonly frontendIpConfigurations!: kulado.Output<{ inboundNatRules: string[], loadBalancerRules: string[], name: string, outboundRules: string[], privateIpAddress: string, privateIpAddressAllocation: string, publicIpAddressId: string, publicIpPrefixId: string, subnetId: string, zones?: string }[] | undefined>;
    /**
     * Specifies the supported Azure Region where the Load Balancer should be created.
     */
    public readonly location!: kulado.Output<string>;
    /**
     * Specifies the name of the frontend ip configuration.
     */
    public readonly name!: kulado.Output<string>;
    /**
     * Private IP Address to assign to the Load Balancer. The last one and first four IPs in any range are reserved and cannot be manually assigned.
     */
    public /*out*/ readonly privateIpAddress!: kulado.Output<string>;
    /**
     * The list of private IP address assigned to the load balancer in `frontend_ip_configuration` blocks, if any.
     */
    public /*out*/ readonly privateIpAddresses!: kulado.Output<string[]>;
    /**
     * The name of the Resource Group in which to create the Load Balancer.
     */
    public readonly resourceGroupName!: kulado.Output<string>;
    /**
     * The SKU of the Azure Load Balancer. Accepted values are `Basic` and `Standard`. Defaults to `Basic`.
     */
    public readonly sku!: kulado.Output<string | undefined>;
    /**
     * A mapping of tags to assign to the resource.
     */
    public readonly tags!: kulado.Output<{[key: string]: any}>;

    /**
     * Create a LoadBalancer resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: LoadBalancerArgs, opts?: kulado.CustomResourceOptions)
    constructor(name: string, argsOrState?: LoadBalancerArgs | LoadBalancerState, opts?: kulado.CustomResourceOptions) {
        let inputs: kulado.Inputs = {};
        if (opts && opts.id) {
            const state = argsOrState as LoadBalancerState | undefined;
            inputs["frontendIpConfigurations"] = state ? state.frontendIpConfigurations : undefined;
            inputs["location"] = state ? state.location : undefined;
            inputs["name"] = state ? state.name : undefined;
            inputs["privateIpAddress"] = state ? state.privateIpAddress : undefined;
            inputs["privateIpAddresses"] = state ? state.privateIpAddresses : undefined;
            inputs["resourceGroupName"] = state ? state.resourceGroupName : undefined;
            inputs["sku"] = state ? state.sku : undefined;
            inputs["tags"] = state ? state.tags : undefined;
        } else {
            const args = argsOrState as LoadBalancerArgs | undefined;
            if (!args || args.resourceGroupName === undefined) {
                throw new Error("Missing required property 'resourceGroupName'");
            }
            inputs["frontendIpConfigurations"] = args ? args.frontendIpConfigurations : undefined;
            inputs["location"] = args ? args.location : undefined;
            inputs["name"] = args ? args.name : undefined;
            inputs["resourceGroupName"] = args ? args.resourceGroupName : undefined;
            inputs["sku"] = args ? args.sku : undefined;
            inputs["tags"] = args ? args.tags : undefined;
            inputs["privateIpAddress"] = undefined /*out*/;
            inputs["privateIpAddresses"] = undefined /*out*/;
        }
        super(LoadBalancer.__kuladoType, name, inputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering LoadBalancer resources.
 */
export interface LoadBalancerState {
    /**
     * One or multiple `frontend_ip_configuration` blocks as documented below.
     */
    readonly frontendIpConfigurations?: kulado.Input<kulado.Input<{ inboundNatRules?: kulado.Input<kulado.Input<string>[]>, loadBalancerRules?: kulado.Input<kulado.Input<string>[]>, name: kulado.Input<string>, outboundRules?: kulado.Input<kulado.Input<string>[]>, privateIpAddress?: kulado.Input<string>, privateIpAddressAllocation?: kulado.Input<string>, publicIpAddressId?: kulado.Input<string>, publicIpPrefixId?: kulado.Input<string>, subnetId?: kulado.Input<string>, zones?: kulado.Input<string> }>[]>;
    /**
     * Specifies the supported Azure Region where the Load Balancer should be created.
     */
    readonly location?: kulado.Input<string>;
    /**
     * Specifies the name of the frontend ip configuration.
     */
    readonly name?: kulado.Input<string>;
    /**
     * Private IP Address to assign to the Load Balancer. The last one and first four IPs in any range are reserved and cannot be manually assigned.
     */
    readonly privateIpAddress?: kulado.Input<string>;
    /**
     * The list of private IP address assigned to the load balancer in `frontend_ip_configuration` blocks, if any.
     */
    readonly privateIpAddresses?: kulado.Input<kulado.Input<string>[]>;
    /**
     * The name of the Resource Group in which to create the Load Balancer.
     */
    readonly resourceGroupName?: kulado.Input<string>;
    /**
     * The SKU of the Azure Load Balancer. Accepted values are `Basic` and `Standard`. Defaults to `Basic`.
     */
    readonly sku?: kulado.Input<string>;
    /**
     * A mapping of tags to assign to the resource.
     */
    readonly tags?: kulado.Input<{[key: string]: any}>;
}

/**
 * The set of arguments for constructing a LoadBalancer resource.
 */
export interface LoadBalancerArgs {
    /**
     * One or multiple `frontend_ip_configuration` blocks as documented below.
     */
    readonly frontendIpConfigurations?: kulado.Input<kulado.Input<{ inboundNatRules?: kulado.Input<kulado.Input<string>[]>, loadBalancerRules?: kulado.Input<kulado.Input<string>[]>, name: kulado.Input<string>, outboundRules?: kulado.Input<kulado.Input<string>[]>, privateIpAddress?: kulado.Input<string>, privateIpAddressAllocation?: kulado.Input<string>, publicIpAddressId?: kulado.Input<string>, publicIpPrefixId?: kulado.Input<string>, subnetId?: kulado.Input<string>, zones?: kulado.Input<string> }>[]>;
    /**
     * Specifies the supported Azure Region where the Load Balancer should be created.
     */
    readonly location?: kulado.Input<string>;
    /**
     * Specifies the name of the frontend ip configuration.
     */
    readonly name?: kulado.Input<string>;
    /**
     * The name of the Resource Group in which to create the Load Balancer.
     */
    readonly resourceGroupName: kulado.Input<string>;
    /**
     * The SKU of the Azure Load Balancer. Accepted values are `Basic` and `Standard`. Defaults to `Basic`.
     */
    readonly sku?: kulado.Input<string>;
    /**
     * A mapping of tags to assign to the resource.
     */
    readonly tags?: kulado.Input<{[key: string]: any}>;
}
